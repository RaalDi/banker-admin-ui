/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import {Lottery} from './lottery';

describe('Lottery', () => {
  it('should create an instance', () => {
    expect(new Lottery()).toBeTruthy();
  });
});
